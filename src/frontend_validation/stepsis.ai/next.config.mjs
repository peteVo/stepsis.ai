/** @type {import('next').NextConfig} */
const nextConfig = {
  // Move this out of the experimental block
  allowedDevOrigins: ['192.168.200.114'],
  
  // Your other config options...
};

export default nextConfig;